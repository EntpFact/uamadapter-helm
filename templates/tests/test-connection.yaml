apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prarambh-um-adapter-helm.fullname" . }}-test-connection"
  labels:
    {{- include "prarambh-um-adapter-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prarambh-um-adapter-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
